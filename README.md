# Fit-Nest

FitNest is a React + TypeScript single-page application that serves as a fitness program enrollment platform connected to a Node.js/Express REST backend. It allows users to enroll in a personalized fitness plan, view all enrolled members, and update or delete existing entries through a clean, form-driven interface.